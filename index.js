/**
 * @format
 */

 import {AppRegistry} from 'react-native';
 import App from './App';
 import {name as appName} from './app.json';
 import glints from './glints';
  AppRegistry.registerComponent(appName, () => glints);
